//Canvas dimensions
var ASPECT_RATIO = 8/6;
var WIDTH = 600;
var HEIGHT = WIDTH / ASPECT_RATIO;

//In game unit (metres) independent of canvas resolution
var UNITS_PER_WINDOW = 20;
var UNIT = Math.round(WIDTH / UNITS_PER_WINDOW);
